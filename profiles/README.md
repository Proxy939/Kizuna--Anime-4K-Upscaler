# Profiles

## Responsibility
Stores JSON/YAML configurations defining processing chains and quality presets.

## Why it exists
Enables data-driven behavior. Allows users to switch between "fast" (shader-only) and "quality" (hybrid/AI) modes without code changes.
